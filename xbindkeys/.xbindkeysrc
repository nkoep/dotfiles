"pgrep -fa ncmpcpp | grep 'osmc$' && mpc -h osmc toggle || mpc toggle"
  Mod2 + F7

"pgrep -fa ncmpcpp | grep 'osmc$' && mpc -h osmc prev || mpc prev"
  Mod2 + F9

"pgrep -fa ncmpcpp | grep 'osmc$' && mpc -h osmc next || mpc next"
  Mod2 + F10

"pactl set-sink-volume @DEFAULT_SINK@ +5%"
  Mod2 + F11

"pactl set-sink-volume @DEFAULT_SINK@ -5%"
  Mod2 + F12
